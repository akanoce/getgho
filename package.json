{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo dev",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@repo/eslint-config": "*",
    "prettier": "^3.1.1",
    "turbo": "latest",
    "dotenv-cli": "latest"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
